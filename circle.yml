machine:
  python:
    version: 3.6.1

dependencies:
  override:
    - pip install .

test:
  override:
    - python setup.py test

deployment:
  lambda:
    branch: [master, migurski/add-s3-upload-loop]
    commands:
      - make live
