machine:
  python:
    version: 3.6.1

dependencies:
  override:
    - pip install .

test:
  override:
    - python setup.py test

deployment:
  production:
    branch: [master]
    commands:
      - make live-lambda
      - make live-website
